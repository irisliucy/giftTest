package com.humannature.www.gift;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
//import android.widget.SeekBar;
//import android.widget.SeekBar.OnSeekBarChangeListener;
//import android.widget.TextView;


public class DonatingPage extends AppCompatActivity {
   // private static SeekBar FirstAmount;
 //   private static TextView TotalAmount;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_donating_page);
    //    seek_bar();

  //  }
//    public void seek_bar() {
     //   FirstAmount = (SeekBar) findViewById(R.id.FirstAmount);
   //     TotalAmount = (TextView)findViewById(R.id.TotalAmount);
    //    TotalAmount.setText(FirstAmount.getProgress());
   //     FirstAmount.setOnSeekBarChangeListener(new OnSeekBarChangeListener() {

   //    int progressA;

  //    @Override
   //        public void onProgressChanged(SeekBar seekBar, int progress, boolean fromUser) {
  //            progressA = progress;
   //           TotalAmount.setText(progress);
    //        }

  //    @Override
   //       public void onStartTrackingTouch(SeekBar seekBar) {

   //         }

   //   @Override
  //        public void onStopTrackingTouch(SeekBar seekBar) {
   //           TotalAmount.setText(progressA);
  //          }
  //       });
    }
}
